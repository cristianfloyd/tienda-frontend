# NPM Configuration for npm 10+
# Optimized for Node 22 and npm 10

# Suppress deprecated warnings during CI/production
loglevel=error
fund=false

# Speed up installs (npm 10 optimizations)
audit=false
optional=false

# Use modern npm features
prefer-offline=true
cache-min=86400